package pr_se.gogame.view_controller;

/**
 * Acts as a wrapper for the actual main class, because the build tools don't like Main Classes that use inheritance.
 */
public class GoApplicationWR {
    public static void main(String[] args) {
        GoApplication.main(args);
    }
}
